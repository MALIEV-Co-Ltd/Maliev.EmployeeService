name: Validate - Main

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-and-test:
    uses: ./.github/workflows/_build-and-test.yml
    with:
      solution_file: Maliev.EmployeeService.slnx
    secrets:
      gitops_pat: ${{ secrets.GITOPS_PAT }}
